<!--EXTERNAL_PROPERTIES: BUILD_BUILDID-->
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <VersionPrefix>1.8.0</VersionPrefix>
    <FileVersion>$(VersionPrefix).$(Revision)</FileVersion>
    <Description>The MPEG-4 Audio extension for AudioWorks. AudioWorks is a cross-platform, multi-format audio conversion and tagging suite.</Description>
    <PackageReleaseNotes>https://github.com/jherby2k/AudioWorks/wiki/MP4-Extension#release-notes</PackageReleaseNotes>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <PackageTags>Windows, MacOS, Linux</PackageTags>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.SourceLink.GitHub" Version="1.1.1" PrivateAssets="all" />
    <PackageReference Include="System.Text.Encoding.CodePages" Version="4.5.1" />
  </ItemGroup>

  <!-- ReSharper disable once UnknownProperty -->
  <ItemGroup Condition="'$(TargetFramework)'=='netstandard2.0'">
    <PackageReference Include="IndexRange" Version="1.0.2" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\AudioWorks.Extensibility\AudioWorks.Extensibility.csproj" PrivateAssets="all" />
  </ItemGroup>

  <Target Name="PublishLocally" AfterTargets="Pack">
    <Exec Condition="'$(OS)'=='Windows_NT'" Command="powershell -ExecutionPolicy Bypass -File &quot;..\PublishLocally.ps1&quot; -ProjectName AudioWorks.Extensions.Mp4 -Configuration $(Configuration)" />
    <Exec Condition="'$(OS)'!='Windows_NT'" Command="pwsh -File &quot;../PublishLocally.ps1&quot; -ProjectName AudioWorks.Extensions.Mp4 -Configuration $(Configuration)" />
  </Target>

</Project>
